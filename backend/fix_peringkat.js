const fs = require('fs'); const path = require('path'); const p = path.join(__dirname, 'data', 'role_permissions.json'); if(fs.existsSync(p)){ const d = JSON.parse(fs.readFileSync(p, 'utf8')); for(const r in d){ if(!d[r]['Peringkat Kelas']){ d[r]['Peringkat Kelas'] = {lihat:false,buat:false,ubah:false,hapus:false}; if(d[r]['Rapor Siswa'] && d[r]['Rapor Siswa'].lihat){ d[r]['Peringkat Kelas'] = {lihat:true,buat:true,ubah:true,hapus:false}; } } } fs.writeFileSync(p, JSON.stringify(d, null, 2)); console.log('Done'); } else { console.log('Not found'); }
