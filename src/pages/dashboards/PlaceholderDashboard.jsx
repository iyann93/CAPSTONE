import AdminDashboard from "./AdminDashboard";
const PlaceholderDashboard = ({ user, activeMenu }) => {
  return <AdminDashboard user={user} activeMenu={activeMenu} />;
};
var PlaceholderDashboard_default = PlaceholderDashboard;
export {
  PlaceholderDashboard_default as default
};
